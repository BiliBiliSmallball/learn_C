/*
第四节课（语法课）:
1.算术符 
*/

#include<sudio.h>
// 算数符：加减乘除
// 有两种版本（单目双目）
// 单目 +(正号) || -(负号)

// 加'减'乘'除'求余;     +,-,*,/,%;
// 求余只有整数,且只能是整数

// 注意:
// 对于整数间的除法(/),除不净将直接舍去小数部分
void NB(){
    double a = 1.666;
    double b = 2.799;
    printf("a为1.666,b为2.799\n");
    printf("a+b = %s;a-b = %s;a*b=%s;a/b = %s;\n",(a+b),(a-b),(a*b),(a/b))
}
